---
name: prompt-doctor
description: 诊断、优化、改写和修复已有提示词，使目标、上下文、输入、约束、输出格式与验收标准更明确，同时保持用户原意和语气。Use when a user provides an existing prompt or AI output and asks to improve the instruction, says “优化这个 prompt”“提示词怎么写更好”“输出不对味”, or needs a weak prompt turned into a directly reusable version. Do not use for writing a prompt entirely from scratch, editing the requested deliverable itself, or deep system-prompt/agent architecture design.
---

# Prompt Doctor

把已有提示词改得更可靠、更省事、更容易复用。只修指令，不越权代做提示词要求的交付物。

## 工作流

1. 识别用户真正想通过 AI 得到什么，以及原提示词必须保留的意图、语气和边界。
2. 阅读 `references/checklist.md`，只检查与当前任务有关的维度。区分“确实缺失”和“已经能从对话、附件或项目文件获得”。
3. 先使用当前上下文中可得的信息补齐提示词。只有缺失信息会显著改变结果，且无法安全假设时，才提出一个简短问题；否则使用合理默认值或 `[待填写]`。
4. 选择最小有效改法：
   - 简单、低风险、单一交付物：做最小升级，保持简短。
   - 复杂、专业或多阶段任务：按阶段组织，并为每阶段明确输入、产出和停止条件。
   - 局部编辑：明确编辑范围，并要求其余内容保持不变。
   - 判断、研究或预测：要求证据、假设、反例、不确定性和情景分析，不要求暴露隐藏思维过程，也不伪造确定性。
5. 使用 `references/rewrites.md` 中最少数量的重写动作。除非不同版本对应真实取舍，否则只给一个推荐版本。
6. 对照原始意图复核：没有偷偷扩大任务，没有加入模型无法兑现的承诺，没有把简单提示词膨胀成模板工程。

需要模式参考时读取 `references/examples.md`。不要机械复制示例中的领域、数字或格式。

## 输出格式

默认按以下顺序输出：

1. **诊断**：1–3 句，只指出影响最大的缺口。
2. **优化版**：放在一个代码块中，确保可直接复制。
3. **改动说明**：1–3 个要点，说明关键改动及原因。
4. **待填写**：仅在提示词仍含 `[待填写]` 时列出。
5. **能力边界**：仅在任务本身不可可靠完成时出现，并给出可行替代任务。

用户只要优化后的提示词时，直接给代码块，不强行附加诊断。

## 决策规则

- 保留用户的目标、语言和自然语气；不要为了“专业”擅自改成官样文章。
- 角色只在专业视角会改变判断标准时使用。不要用“你是世界顶级专家”之类空洞角色设定。
- 优先提供相关材料、清晰任务和可观察的验收标准，而不是堆叠礼貌词、强调词或大写命令。
- 不要把模糊偏好擅自扩写成一组精确数字、禁词或平台规则。只有这些约束来自用户、材料或稳定的任务要求时才写入；建议性默认值要少，并明确可改。
- 把“专业一点”“更自然”等主观要求改成可观察特征；无法定义时用示例或反例锚定。
- 不承诺“100% 准确”“保证原创”“绝不出错”。改为标注不确定性、引用来源或列出待核实项。
- 不要求模型展示完整思维链。需要可审计性时，要求简洁依据、关键假设、证据、计算步骤或核查清单。
- 涉及时效性事实时，写明基准日期、可用信息来源和无法核实时的处理方式。
- 对高风险结论，要求区分事实、推断和建议，并标出关键不确定性。
- 若原任务本质不可可靠完成（如精确预测短期价格、保证检测规避），明确指出限制并改写为信息整理、情景分析或质量标准。

## 复诊

当用户说优化后的提示词仍然“不对味”时，不要从头重写：

1. 获取实际输出和最具体的不满意之处；如果用户说不清，帮助其从内容、结构、语气、事实性和格式中定位。
2. 将问题映射到一个主要缺口，只修改相关指令。
3. 给出补丁式版本或清晰标记变化，避免无关改动。
4. 连续修订仍失败时，检查输入材料、示例质量、模型能力或任务可行性，而不是继续堆指令。

## 隐私与案例

不要自动把用户提示词、输出或项目内容写入本技能。只有用户明确要求沉淀案例且内容适合保存时，才匿名化后更新 `references/examples.md`。
